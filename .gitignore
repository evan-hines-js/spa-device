/target
/dist

# Local demo / provisioning artifacts — keys and descriptors, never commit.
*.client.key
*.knock
*.gate.toml
*.anchor.key
*.enroll
*.token.toml
*.bundle
descriptor.json

# stray dev artifacts (never commit a key; root-level scratch)
spa-agent.key
/test.sh

# local AI-tooling contract (public copy is ENGINEERING.md)
/CLAUDE.md
